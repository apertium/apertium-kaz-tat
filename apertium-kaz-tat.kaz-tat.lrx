<rules>
  <rule>
    <match lemma="эт"><select lemma="эт"/></match>
  </rule>

  <rule> <!-- FIXME: seemes not to work --> 
    <match lemma="Франция" tags="np.top.*"/>
    <match lemma="құрама" tags="adj.subst.*"><select lemma="җыелма" tags="adj.subst.*"/></match>
  </rule>

  <rule>
    <match lemma="күрделі" tags="adj.*"/><select lemma="кушма" tags="adj"/>
    <match lemma="сөз" tags="n.*"/></match>
  </rule>

</rules>
